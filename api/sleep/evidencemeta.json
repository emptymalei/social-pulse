{"queries":[{"id":"sleep","compiledQueryString":"select * from health.sleep_daily_data","inputQueryString":"select * from health.sleep_daily_data","compiled":false,"inline":true},{"id":"selected_sleep_year","compiledQueryString":"select date, year, total_hours, total_hours_7_day_avg from health.sleep_daily_data\nwhere year in ${inputs.selected_sleep_years.value}","inputQueryString":"select date, year, total_hours, total_hours_7_day_avg from health.sleep_daily_data\nwhere year in ${inputs.selected_sleep_years.value}","compiled":false,"inline":true},{"id":"year_stats","compiledQueryString":"select\n    year,\n    count(*) as days_recorded,\n    round(avg(total_hours), 2) as avg_hours,\n    round(min(total_hours), 2) as min_hours,\n    round(max(total_hours), 2) as max_hours,\n    round(stddev(total_hours), 2) as std_dev\nfrom health.sleep_daily_data\nwhere year in ${inputs.selected_sleep_years.value}\ngroup by year\norder by year desc","inputQueryString":"select\n    year,\n    count(*) as days_recorded,\n    round(avg(total_hours), 2) as avg_hours,\n    round(min(total_hours), 2) as min_hours,\n    round(max(total_hours), 2) as max_hours,\n    round(stddev(total_hours), 2) as std_dev\nfrom health.sleep_daily_data\nwhere year in ${inputs.selected_sleep_years.value}\ngroup by year\norder by year desc","compiled":false,"inline":true}]}